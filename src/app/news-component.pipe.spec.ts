import { NewsComponentPipe } from './news-component.pipe';

describe('NewsComponentPipe', () => {
  it('create an instance', () => {
    const pipe = new NewsComponentPipe();
    expect(pipe).toBeTruthy();
  });
});
