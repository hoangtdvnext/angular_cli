import { NewsComponentDirective } from './news-component.directive';

describe('NewsComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsComponentDirective();
    expect(directive).toBeTruthy();
  });
});
